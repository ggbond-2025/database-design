spring:
  datasource:
    driver-class-name: org.opengauss.Driver
    url: ${DB_URL:jdbc:opengauss://192.168.126.131:8888/dengjxmis13}
    username: ${DB_USERNAME:gaussdb}
    password: ${DB_PASSWORD:@Djx1234}
  jackson:
    time-zone: Asia/Shanghai

logging:
  level:
    root: warn
    org.opengauss: warn

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-change-this-secret}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:120}
